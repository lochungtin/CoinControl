
export const update = data => {
    var output;

    Object.keys(data).forEach(day => Object.keys(data[day]).forEach(key => {
        const record = data[day][key];

        
    }));

    return output;
}