import { Dimensions, StyleSheet } from 'react-native';

export const bgColor = '#F7F7FF';

export const styles = StyleSheet.create({
    screen: {
        alignItems: 'center',
        backgroundColor: bgColor, 
        flex: 1,
        justifyContent: 'flex-start',
    },
});